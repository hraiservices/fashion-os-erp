// Same widget as /login, just defaulted to signup mode — see LoginPage's pathname check.
// One shared component instead of duplicating the whole glass-card form.
export { default } from "@/app/login/page";
