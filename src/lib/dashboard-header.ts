export interface DashboardHeaderConfig {
  showClock: boolean;
  showDate: boolean;
  showGlobe: boolean;
  showFlag: boolean;
}

export const DEFAULT_DASHBOARD_HEADER_CONFIG: DashboardHeaderConfig = {
  showClock: true,
  showDate: true,
  showGlobe: true,
  showFlag: true,
};
